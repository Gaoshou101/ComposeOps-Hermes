PORT=3001
HOST=0.0.0.0
LOG_LEVEL=info
ENABLE_SHELL=1
# ADMIN_PASSWORD=至少10个字符（可选；推荐通过首次启动页面设置）
# DISABLE_BACKGROUND_JOBS=1
# SERVE_FRONTEND=1   # 生产环境设为 1 以托管前端 dist
# COMPOSE_BIN=docker # 'docker' -> 'docker compose'; 旧版可用 'docker-compose'
