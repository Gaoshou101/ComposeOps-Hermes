# 需求

- 登录后的每个运维页面都能从顶部打开页面 Agent。
- Agent 请求必须携带当前路由、页面摘要、可见状态和表单字段，并过滤敏感字段。
- 定时任务新建弹窗可直接打开 Agent；Agent 通过现有确认机制调用 `cron.create` 后关闭弹窗并刷新列表。
- 页面接口返回不完整时不得因读取缺失的 `total` 字段导致组件异常。
- 保持现有 Agent SSE、会话、工具确认和权限模型不变。
