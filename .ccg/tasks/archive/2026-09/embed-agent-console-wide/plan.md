# 实施计划

1. 检查现有 Agent SSE、全局布局、定时任务流程和统计响应归一化。
2. 在全局布局挂载 Agent 抽屉，并通过页面快照跟踪让所有路由共享上下文。
3. 给定时任务编辑器增加 Agent 入口、表单上下文同步和创建完成事件处理。
4. 后端接收并注入页面上下文，同时保留工具确认和权限校验。
5. 为缺失统计字段增加安全默认值，覆盖 `total` 访问路径。
6. 添加页面上下文与定时任务集成测试，运行全量测试、构建、语法检查和双模型审查。
